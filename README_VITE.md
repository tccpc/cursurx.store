# 使用 Vite 快速开始

本项目已集成 Vite 构建系统，提供极速的开发体验。

## 🚀 快速命令

```bash
# 1. 安装依赖（首次运行）
npm install

# 2. 启动开发服务器
npm run dev
# 或
npm start

# 3. 构建生产版本
npm run build

# 4. 预览生产构建
npm run preview
```

## 📊 命令对比

| 操作 | 旧方式 | Vite 方式 | 优势 |
|------|--------|-----------|------|
| 开发 | `python -m http.server 8000` | `npm run dev` | ⚡ HMR 热更新 |
| 构建 | 无 | `npm run build` | 📦 优化压缩 |
| 预览 | 手动打开 HTML | `npm run preview` | 🎯 生产环境预览 |

## ⚡ Vite 优势

### 开发环境
- 🚀 **极速启动**：< 1 秒启动开发服务器
- 🔥 **热模块替换**：修改代码即时生效，无需刷新
- 📦 **按需编译**：只编译当前需要的模块
- 🐛 **更好的错误提示**：清晰的错误堆栈

### 生产环境
- 📦 **代码分割**：自动拆分代码，按需加载
- 🗜️ **高效压缩**：使用 Terser 压缩 JavaScript
- 🌳 **Tree Shaking**：移除未使用的代码
- 🎯 **文件哈希**：便于长期缓存
- 📊 **构建优化**：CSS 提取、静态资源优化

## 📁 构建输出

运行 `npm run build` 后：

```
dist/
├── index.html               (压缩后)
├── models.html              (压缩后)
├── terms.html               (压缩后)
└── assets/
    ├── index-abc123.js      (带哈希的 JS)
    ├── index-def456.css     (提取的 CSS)
    ├── config-ghi789.js
    ├── i18n-jkl012.js
    └── script-mno345.js
```

所有文件自动优化、压缩、添加哈希值。

## 🔧 自定义配置

编辑 `vite.config.js`：

```javascript
export default defineConfig({
  server: {
    port: 3000,              // 修改开发服务器端口
    open: true,              // 自动打开浏览器
  },
  build: {
    outDir: 'dist',          // 修改输出目录
    sourcemap: true,         // 生成源码映射
  }
});
```

## 🌍 环境变量

创建 `.env.local` 文件（参考 `env.example`）：

```bash
VITE_API_URL=https://api.example.com
VITE_SITE_NAME=Cursor X
```

在代码中使用：

```javascript
const apiUrl = import.meta.env.VITE_API_URL;
```

**注意：** 只有以 `VITE_` 开头的变量才会暴露给客户端！

## 📚 更多信息

- 详细使用指南：[VITE.md](VITE.md)
- 部署说明：[DEPLOYMENT.md](DEPLOYMENT.md)
- 项目文档：[README.md](README.md)

---

**提示：** 开发时使用 `npm run dev`，部署前先运行 `npm run build` 构建生产版本！

